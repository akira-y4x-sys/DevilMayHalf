	bob
	
	var is_moving = input_dir.length() > 0.1
	if is_moving and is_on_floor():
		bob_timer += delta * bob_speed
	else:
		bob_timer = lerp(bob_timer, 0.0, delta * 5)
	var bob_offset = sin(bob_timer) * bob_amount
	bob.position.y = bob_offset
